import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.testappecommercial.reactsync',
  appName: 'demo-commercial-web-app',
  webDir: 'dist'
};

export default config;
